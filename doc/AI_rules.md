# AI Coding Rules

Most important :- before any of the task first alweays read the ai rules file what to every "AI_RULES.md"

Never change architecture.

Never rename existing APIs.

Never rename terminology.

Prefer existing utilities before creating new ones.

Keep code modular.

Avoid duplication.

Use JavaScript everywhere.

Use async/await.

Never hardcode secrets.

Never modify documentation unless requested.

If uncertain, ask instead of assuming.

make config file to config the project with ease insted of hardcoding thing in both frontend and backend 

Editting in the doc folder is not allowed untill explicetly told by the the user you can edit the notes foder files which is inide the doc not the othere file which are the core doc of the project 

keep security in mind will doing thing it will be used in production and by real users